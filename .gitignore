.venv/
build/
dist/
__pycache__/
*.pyc

# Model weights are committed on purpose so a clone is ready to run.

# Test footage: contains real faces, and is large. Keep it local.
test/
*.mp4
*.mov
*.mkv
