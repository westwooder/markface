# markface 使用说明

选一个视频，自动给画面里的人脸打上马赛克。

下载解压就能用，**不需要装 Python，不需要装任何依赖库**。

---

## 一、安装

1. 下载 `markface-v1.0-windows-x64.zip`
2. 解压到任意位置，比如 `D:\markface`
3. 双击文件夹里的 `markface.exe`

就这样，没有安装步骤。

**注意事项**

- 解压路径不要放在 C 盘根目录或「Program Files」里面，那些位置可能没有写入权限。
- 第一次运行 Windows 可能弹出「已保护你的电脑」提示，这是因为程序没有购买代码签名证书。点「更多信息」→「仍要运行」。
- 杀毒软件偶尔会对 PyInstaller 打包的程序报误判，加白名单即可。
- 系统要求：Windows 10 或 11，64 位。

---

## 二、三步出结果

1. **选视频** —— 点「浏览…」，或者直接把视频文件拖到窗口里。
   输出路径会自动填成「原文件名_马赛克.mp4」，也可以点「另存为…」改。

2. **看预览** —— 点「预览当前帧」。拖动预览区下面的进度条可以换画面，
   挑一个有人脸的地方，确认马赛克的位置和大小合适。

3. **开始打码** —— 点右下角蓝色的「开始打码」。

进度条分两段：前半段是「分析」（逐帧找人脸），后半段是「输出」（写新视频）。
完成后会弹窗告诉你打了多少帧，点「打开输出目录」直接看结果。

---

## 三、界面说明

### 常用

**加速方式**

| 选项 | 说明 |
|---|---|
| 自动选择 | 有显卡就用显卡，没有就用 CPU。一般选这个 |
| GPU 加速 | 强制用显卡。支持 NVIDIA、AMD、Intel（通过 DirectML） |
| CPU | 不用显卡。兼容性最好，但慢很多 |

**识别强度**

| 选项 | 说明 |
|---|---|
| 快速 | 小模型，速度最快。遮挡场景可能漏 |
| 均衡 | 默认。逐帧检测，误检少，日常够用 |
| 严格 | 大图输入 + 低阈值，并自动开启 YuNet、姿态推断、遮挡补偿。**蒙眼、堵嘴、戴头套这类重遮挡素材用这档**，速度约慢一半 |

**马赛克范围** —— 这几个滑块调完可以立刻点预览看效果。

| 滑块 | 默认 | 作用 |
|---|---|---|
| 横向范围 | 1.00× | 马赛克的宽度，相对于检测到的人脸框 |
| 纵向范围 | 1.02× | 高度。**眼睛露出来就调大这个** |
| 上下位置 | −0.05 | 负值往上移，正值往下移 |
| 马赛克粗细 | 0.085 | 格子的基准大小 |
| 遮挡厚度 | 1.2× | **觉得码不够厚就调这个**，最大 3.0× |
| 压暗程度 | 0.10 | 让码块看起来是实色块，而不是一团模糊 |
| 柔化程度 | 0.12 | 调到 0 是纯像素块，调大趋近模糊 |
| 边缘羽化 | 6px | 边缘过渡，避免生硬的贴片感 |
| 形状 | 椭圆 | 椭圆贴合脸型；也可以选矩形 |

两个容易困惑的地方：

- **为什么眼睛会露出来？** 椭圆在左右两侧比中间矮，而眼角正好靠外侧。
  所以露眼睛通常不是整体太小，而是要把「纵向范围」调大，或「上下位置」调更负。
- **「遮挡厚度」和「马赛克粗细」有什么区别？** 粗细是格子的基准尺寸；
  厚度按倍数放大格子，同时在取格子平均色之前先压平细节。不压平的话，
  睫毛、眼镜框这类高对比线条会把所在格子的平均色拽偏，五官轮廓会隐约透出来。

### 检测与跟踪

**检测器**

| 选项 | 默认 | 说明 |
|---|---|---|
| 人脸阈值 | 0.25 | 越低找得越多，误检也越多 |
| YuNet 二次检测 | 关 | 另一套架构的检测器，补主模型的漏。几乎不耗时 |
| 姿态推断头部 | 关 | 脸完全看不见时（背对镜头、戴头套），靠身体关键点定位头部。**需要画面里能看到身体**，纯脸部特写用不上 |
| 遮挡补偿 | 关 | 检测框因遮挡缩成一条时自动补全 |
| 过滤误检 | 开 | 去掉手臂、肩膀这类平坦皮肤的误判 |

后三项默认关闭是为了减少误检。遇到蒙眼堵嘴的素材，直接把「识别强度」切到
**严格**，会一次性全开，不用手动勾。

**跟踪与补帧**

| 选项 | 默认 | 说明 |
|---|---|---|
| 启用跟踪 | 关 | **如果发现有漏帧（某些画面没打上码），打开它** |
| 丢失后续遮 | 10 帧 | 人脸检测不到后，按运动趋势继续打码多少帧 |
| 断档插补 | 12 帧 | 整段分析完后，回填中间漏掉的帧 |
| 平滑窗口 | 5 帧 | 让马赛克位置不抖 |
| 检测间隔 | 1 | 每几帧检测一次。设 1 最稳 |

跟踪的代价是：人已经走出画面后，可能还会在原位置多留几帧马赛克。所以默认关闭，
发现漏帧再开。

### 输出

| 选项 | 默认 | 说明 |
|---|---|---|
| 保留原视频声音 | 开 | 音轨直接复制，不重新编码，音质无损 |
| 画质 | 跟随原视频 | 读取源文件的编码器、码率、像素格式，按同规格输出 |
| CRF 值 | 18 | 只在画质选「指定 CRF」时生效。数字越小越清晰、文件越大 |
| CPU 线程数 | 自动 | 一般不用改 |

「跟随原视频」的含义：源文件是 HEVC 2000kbps，输出也是 HEVC，码率给到约 2300kbps
（留 15% 余量）。画面观感与原片一致。

---

## 四、常见问题

**有些画面没打上码**

按这个顺序试：

1. 打开「检测与跟踪」→ 勾上「启用跟踪」
2. 「识别强度」切到「严格」
3. 还漏就把「人脸阈值」往下调，比如 0.15

**打了多余的码，脸以外的地方也有**

1. 确认「过滤误检」是勾上的
2. 把「人脸阈值」往上调，比如 0.4
3. 如果开了「姿态推断头部」，关掉试试 —— 它在没有身体的特写镜头上容易误判

**眼睛/嘴巴露在马赛克外面**

调大「纵向范围」（比如到 1.15），或把「上下位置」调更负（比如 −0.10）。
先点「预览当前帧」看效果，不用每次都完整跑一遍。

**马赛克不够厚，还能看出是谁**

调大「遮挡厚度」，1.2× → 2.0× 试试。也可以同时调大「马赛克粗细」和「压暗程度」。

**处理很慢**

- 「加速方式」确认不是选了 CPU
- 「识别强度」从「严格」换成「均衡」或「快速」
- 「检测间隔」从 1 改成 2（配合打开跟踪）

参考速度：1080p 视频，Intel Arc 显卡，「均衡」档约每秒处理 7 帧。CPU 慢 3-5 倍。

**程序一闪就没了**

打开「输出」标签页 →「打开日志目录」，里面 `markface.log` 有错误信息。
把日志内容反馈给我们。

**想恢复默认设置**

删掉 `%APPDATA%\markface\settings.json`，下次启动就是默认值。

---

## 五、文件位置

| 内容 | 路径 |
|---|---|
| 设置 | `%APPDATA%\markface\settings.json` |
| 日志 | `%APPDATA%\markface\markface.log` |
| 模型 | 程序目录下 `_internal\models\` |

（在资源管理器地址栏输入 `%APPDATA%\markface` 可以直接打开。）

---

## 六、隐私说明

程序完全在本地运行，不联网，不上传任何视频或画面数据。模型文件已经打包在
程序里，运行时也不会去下载东西。
