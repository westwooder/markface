# CLAUDE.md

给之后在这个项目上工作的 AI 助手和开发者。记录的是**代码里看不出来、需要实测才知道**
的约束和结论，不重复 README 已有的内容。

交流用中文，代码注释用英文，文件存 UTF-8。

## 项目定位

Windows 桌面程序，给视频里的人脸打马赛克。核心诉求按优先级：

1. **宁误杀不放过** —— 漏一张脸是失败，多打一块码只是难看
2. **不漏帧** —— 视频里任何一帧没遮住都等于没做
3. **遮挡也要认出来** —— 蒙眼、堵嘴是明确要支持的场景
4. **马赛克要留头部轮廓，只遮五官**，块状而非纯模糊（纯模糊"看着惊悚"）

打包必须自带 Python 环境，目标机器零依赖。

## 踩过的坑（重要，别重复踩）

### DirectML 不能并发创建会话

**症状**：程序闪退，无 Python traceback。Windows 事件日志显示
`onnxruntime_pybind11_state.pyd` 访问违例 `c0000005`。

**触发路径**：拖动预览滑块 → 多个预览线程重叠 → 每个线程各建一套 DirectML 会话 → 崩。

**隔离验证的结论**：串行建 5 个 DML 会话全部通过，并发建 5 个立即段错误。
不是 GC 问题（我一开始误判为 QThread 被回收，改完仍崩）。

**解法**：`detect/runtime.py` 里的 `GPU_LOCK`（全局 RLock）串行化**会话创建和推理**
两件事；`detect/cache.py` 缓存 ensemble，改阈值复用会话，只有换模型/设备才重建。

改这块代码时务必保持：任何 DML 推理都要走 `run_locked()`，不要直接 `sess.run()`。

### OpenCV 随机定位会静默失效

**症状**：预览滑块拖到不同位置，显示的始终是同一帧画面。

**原因**：某些容器（实测 `test/20260721_170951695.mp4`）`cap.set(CAP_PROP_POS_FRAMES, n)`
之后 `cap.get(CAP_PROP_POS_FRAMES)` 返回 **-4.0**（无效值），且 `read()` 返回的是
同一张图。`CAP_PROP_POS_MSEC` 定位同样失效。不报错，静默返回错数据。

**解法**：`video.py` 的 `FrameReader._check_seekable()` 先探测一次，失效就退化为
"重开文件 + 顺序读到第 n 帧"。慢，但正确。

### 重复像素化不能让马赛克变厚

用户反馈"码不够厚"，我第一版实现是重复降采样 N 次（`passes` 参数）。**无效** ——
区域一旦压平，再取一次平均得到的是同样的值。实测细节量（Laplacian 方差）
`passes=1` → 341，`passes=3` → 337，基本没变。

**真正有效的两件事**：

1. **放大色块**（`strength` 乘在 `block_ratio` 上）
2. **取格子均值之前先按格子尺度模糊一次**（`flatten`）

第 2 点容易被忽略但很关键：不预模糊的话，睫毛、蒙眼布边缘这类高对比线条会把所在
格子的均值拽偏，五官轮廓仍隐约透过色块。实测 Laplacian 从 248 降到 162。

`passes` 参数已废弃，换成 `strength` + `flatten`。

### 椭圆在两侧比中间矮，眼睛容易漏在外面

用户反馈"眼睛还是露出来了"。按椭圆中轴算，眼睛在脸框 32% 高度处是被覆盖的 ——
但**眼角在左右两侧**，椭圆那里高度小得多。

所以"眼睛露出来"通常不是整体太小，而是要：调大 `scale_y`（到 1.02 以上，允许超出
检测框）或把 `offset_y` 调更负。别只调 `scale_x`。

### 检测器在遮挡时会缩框

实测数据（合成遮挡视频，同一张脸）：

| 遮挡程度 | YOLO 框 | YuNet | pose |
|---|---|---|---|
| 无遮挡 | 56×82 | 56×84 | 40×33 |
| 蒙眼 | 54×51 | 59×76 | 26×21 |
| 蒙眼+堵嘴 | 52×29 | 失效 | 失效 |

框高从 82px 缩到 29px，**只剩额头那一条**。光按检测框打码会露出整个下半张脸。

`detect/occlusion.py` 两重补偿：按正常脸部长宽比（1.32）把扁框补高；跟踪链记住
历史最大框，不允许缩到 72% 以下。

### 一张脸被打两个椭圆

**原因**：YOLO 和 YuNet 框同一张脸的方式不同（YOLO 瘦高、YuNet 矮胖），IoU 可能
只有 0.40，低于 NMS 阈值 0.55 就双双保留。

**解法**：`boxes.py` 的 `same_face()` 按**中心距离相对于框尺寸**判断，而不是重叠面积 ——
同一张脸的两个读数中心必然接近，不管长宽比差多少。判定为同脸则取并集。

还有个顺序陷阱：`repair_aspect()` 必须在合并**之前**跑，否则比较用的是被遮挡压扁的
尺寸，面积比不够就合不上。

### 身体皮肤被误判成脸

多余的框出现在画面 56% 高度处，肤色占比 49-68%，但细节量只有 18-24（真脸 32-44）。
是手臂、肩膀这类平坦皮肤。

`detect/verify.py`：置信度低**且**内部无纹理的框丢掉。高置信度的框不做这个检查，
所以蒙眼堵嘴的脸不受影响（遮眼布有边缘、头部有轮廓，细节量够）。

### 改默认值会被用户的旧配置覆盖

用户跑过一次后 `settings.json` 就存在了，`Settings.load()` 会用里面的旧值，改代码
里的默认值不生效。

`settings.py` 的 `SCHEMA_VERSION`：版本不匹配就丢弃旧文件（备份成 `.json.bak`）
并用新默认值。**改任何默认值都要 bump 这个版本号**，否则老用户看不到变化。

## 架构要点

### 为什么是两趟处理

`pipeline.py`：第一趟逐帧检测 + 跟踪，第二趟渲染。不是为了省内存，是为了**能回头看** ——
单趟流式只能按已知帧向前外推，两趟可以用后面的检测结果插值回填中间漏掉的帧
（`track/smooth.py` 的 `fill_gaps`）。

### 检测是可选的集成，不是全开

三个检测器（YOLO 主模型 / YuNet / pose）默认只开主模型。YuNet 和 pose 在
「严格」预设里才全开。原因：全开时误检明显增多，而多数素材不需要。

`PRESETS` 里 `thorough` 是唯一开启 `use_yunet` / `use_pose` / `repair_occlusion`
的档位 —— 遇到重遮挡素材让用户切这一档，而不是手动勾三个框。

### pose 模型的适用边界

`yolo26n-pose` 靠身体关键点反推头部，所以**画面里必须能看到身体**。纯脸部特写
（人脸占满画面）出不了关键点，实测 32 个特写帧拿到 0 个关键点。别指望它救特写镜头。

### 输出画质跟随源文件

`video.py` 的 `probe_streams()` 解析 `ffmpeg -i` 的 stderr 拿编码器/码率/像素格式
（imageio-ffmpeg 不带 ffprobe）。HEVC 源用 libx265 编回 HEVC，码率给源的 1.15 倍。
音轨 `-c:a copy` 直接复制不重编码。

注意：有些文件**音频流在前、视频流在后**（`test/20260721_170951695.mp4` 就是），
解析码率时要取 `Video:` 那一行，别取容器总码率。

### 检测框只画在预览里

`apply_mosaic(..., draw_boxes=...)` 是显式参数，不读 settings。渲染路径不传它，
所以输出文件**永远不可能带彩色框**，即使用户勾了「显示检测框」。这是刻意设计，
别改成从 settings 里读。

## 当前默认值（schema 3）

用户明确要求过的取值，改动前先确认是不是他们定的：

| 设置 | 值 | 备注 |
|---|---|---|
| `strength` | 1.2 | 用户指定 |
| `scale_x` / `scale_y` | 1.00 / 1.02 | scale_y > 1 是为了盖住两侧的眼角 |
| `offset_y` | −0.05 | 用户要求"往上一点" |
| `block_ratio` | 0.085 | |
| `use_yunet` / `use_pose` / `repair_occlusion` | 全 False | 用户要求默认关闭 |
| `track_enabled` | False | 用户要求关闭，界面提示"漏帧就开" |
| `track_max_age` | 10 帧 | 用户指定 |
| `reject_flat` | True | 过滤平坦皮肤误检 |
| `quality_mode` | match | 跟随源文件编码 |

## 开发与构建

```powershell
# 源码运行
.venv\Scripts\python.exe main.py

# 命令行（便于脚本化验证，不走 GUI）
.venv\Scripts\python.exe tools\cli.py in.mp4 out.mp4 --preset thorough --device cpu

# 打包
powershell -ExecutionPolicy Bypass -File build.ps1
```

`tools/cli.py` 是验证改动最快的方式 —— GUI 不好自动化，命令行能直接跑完整管线。

调试 GUI 用 `QT_QPA_PLATFORM=offscreen`，可以无窗口跑完整流程。

### 打包注意

- `markface.spec` 里 `_drop_duplicate_ffmpeg()` 去掉 imageio_ffmpeg 自带的那份
  ffmpeg（84MB 重复）。产物 507MB，压缩后 249MB。
- 验证打包结果**必须在清空 PATH 的环境里跑**，否则可能悄悄用了系统 Python：
  `$env:PATH='C:\Windows\system32;C:\Windows'` 再启动 exe。
- 模型已提交进 git（125MB）。上游 HuggingFace 在国内直连不通，clone 下来就能跑
  比省仓库体积更重要。`tools/fetch_models.py --mirror hf-mirror.com` 可走镜像。

## 许可约束（重要）

项目是 **AGPL-3.0**，这不是选择而是义务：

- `yolov11s/m-face` 派生自 akanametov/yolo-face → **GPL-3.0**
- `yolo26n-pose` 基于 Ultralytics → **AGPL-3.0**

两者传染。**如果要换成宽松许可，必须替换这两个模型**（YuNet 是 MIT，可以留）。
仅在界面上关掉检测器不够 —— 主模型本身就是 GPL 的。

AGPL 额外一条：做成在线服务时使用者有权索取服务端源码。详见 `NOTICE.md`。

## 测试素材

`test/` 目录有真人视频，**已在 .gitignore 里排除**，不要提交（含人脸，且 77MB）。

`tools/make_test_video.py` 可以从一张静态图合成带遮挡的测试视频（中段蒙眼、
后段蒙眼+堵嘴），用来确定性地验证遮挡路径。但要注意：合成视频把脸贴在纯色背景上，
pose 模型在上面会乱报，别用它评估误检率 —— 误检要用真实素材测。

## 当前已知问题

- 关闭跟踪时，`test/20260721_170951695.mp4` 有 59 帧（22%）检测不到人脸，集中在
  105-124 这类连续区间。打开跟踪或切「严格」可缓解。
- 抽样检查输出时，`video_20260501` 有 2 帧存在两个打码区域（上下相距 300+px）。
  未确认是误检还是画面里真有第二个人，保守起见保留了。
